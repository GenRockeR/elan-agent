#!/usr/bin/env python

import time
from origin.nac import FirewallConfigurator

if __name__ == "__main__":
    conf = FirewallConfigurator()
    count = 1
    while count < 100:
        conf.run()
        time.sleep(5)
        count = count + 1
