#!/usr/bin/env python3

import time
from origin.captive_portal import GuestAccessManager

if __name__ == "__main__":
    manager = GuestAccessManager()
    count = 1
    while count < 100:
        manager.run_for_ever()
        time.sleep(5)
        count = count + 1
