ifdown -a --exclude=lo
# Generate default net configuration if needed
if[ ! -e /etc/network/interfaces.d/ea-network ]
PYTHONPATH=/origin/lib/python python3 -c 'import origin.network; conf = origin.network.NetworkConfiguration(); conf.generate_configuration_file()'
ifup -a
exit 0
