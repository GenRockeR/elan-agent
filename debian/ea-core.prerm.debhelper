# Automatically added by dh_installinit
if [ -x "/etc/init.d/nginx" ] || [ -e "/etc/init/nginx.conf" ]; then
	invoke-rc.d nginx stop || exit $?
fi
# End automatically added section
# Automatically added by dh_installinit
if [ -x "/etc/init.d/axon" ] || [ -e "/etc/init/axon.conf" ]; then
	invoke-rc.d axon stop || exit $?
fi
# End automatically added section
