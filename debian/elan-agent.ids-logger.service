[Unit]

Description=IDS logger to Central Manager

[Service]
Restart=always
RestartSec=10

Environment=PYTHONPATH=/origin/lib/python

ExecStartPre=/bin/mkdir -p /var/log/suricata
ExecStart=/origin/bin/ids-loggerd

[Install]
WantedBy=multi-user.target
