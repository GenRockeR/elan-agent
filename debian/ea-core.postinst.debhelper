# Automatically added by dh_installinit
if [ -x "/etc/init.d/axon" ] || [ -e "/etc/init/axon.conf" ]; then
	if [ ! -e "/etc/init/axon.conf" ]; then
		update-rc.d axon defaults >/dev/null
	fi
	invoke-rc.d axon start || exit $?
fi
# End automatically added section
# Automatically added by dh_installinit
update-rc.d -f axon remove >/dev/null || exit $?
# End automatically added section
# Automatically added by dh_installinit
if [ -x "/etc/init.d/nginx" ] || [ -e "/etc/init/nginx.conf" ]; then
	if [ ! -e "/etc/init/nginx.conf" ]; then
		update-rc.d nginx defaults >/dev/null
	fi
	invoke-rc.d nginx start || exit $?
fi
# End automatically added section
# Automatically added by dh_installinit
update-rc.d -f nginx remove >/dev/null || exit $?
# End automatically added section
