description "DHCP fingerprinter and Host gatherer"

start on filesystem or runlevel [2345]
stop on runlevel [!2345]

respawn
respawn limit 10 5

env PYTHONPATH=/origin/lib/python

exec /origin/bin/session-trackerd
